// Exerciase 05-B: Phone Combinations

#include <iostream>
#include <map>
#include <string>
#include <vector>

// Constants

// Functions

void phone_combinations(std::string numbers, std::string letters) {
    // TODO: Recursively display combinations
}

// Main execution

int main(int argc, char *argv[]) {
    std::string numbers;

    while (std::getline(std::cin, numbers)) {
        phone_combinations(numbers, "");
    }

    return 0;
}
